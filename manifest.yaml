#© 2021 Sean Murdock

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sean-stedi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tucker-stedi
  template:
    metadata:
      labels:
        app: tucker-stedi
    spec:
      containers:
        - name: tucker-stedi
          image: gcr.io/teca-350016/stedi-tucker:latest
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: ".5"
              memory: "500Mi"
            requests:
              cpu: ".25"
              memory: "250Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 3